---
- hosts: localhost
  tasks:
    - name: Create install-config.yaml file
      template: 
        src: /home/ocp_userid/install-config.j2
        dest: /home/ocp_userid/install-config.yaml
        owner: ocp_userid
        group: ocp_userid
        mode: "0644"
