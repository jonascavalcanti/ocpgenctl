version: '3.7'
services:
  ocp-bootstrap:
    build: .
    container_name: ocp-bootstrap
    hostname: ocp-bootstrap
    restart: always
    network_mode: host
    ports:
      - 80:80
    volumes: 
      - ${HOME}/ocp/sharedfolder:/home/ocp3900/sharedfolder
          